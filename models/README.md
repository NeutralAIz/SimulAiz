Place offline model assets here and mount into the container at /models.

Examples:
- XTTS‑v2: /models/xtts/{config.json, model.pth, vocab.json}
- LivePortrait: /models/liveportrait/ (if you later integrate a bridge)
